import { PurchaseOrderPipePipe } from './purchase-order-pipe.pipe';

describe('PurchaseOrderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseOrderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
